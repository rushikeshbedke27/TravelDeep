This is the updated version the Travel Model:

To run this project follow below steps:

Step1: Activate tourismDeep_venv virtual Environment(venv)
	tourismDeep_venv\Scritps\activate

Step2: Install all requirements.
	pip install -r requirements.txt

Step3: Run the Project:
	streamlit run app.py

Step4: i. After running the project please select the path for file "best_model.h5" and "final_df.csv".

Step5: Then click on Initialize System to run the System.